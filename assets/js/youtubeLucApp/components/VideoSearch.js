import React from 'react';

const VideoSearch = props => (
	<form onSubmit={props.getVideo}>
		{/* <input type="text" name="videoInfo" />
		<button>Search</button> */}
	</form>
);

export default VideoSearch;
